import { SectionWithImageAndCTA } from "./common";

// Call-to-action section configuration
// Used for prominent promotional sections with imagery and action buttons
export type IodCallToActionSection = SectionWithImageAndCTA;
